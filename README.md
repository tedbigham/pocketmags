# Magazine BIN → PDF (Firefox Extension)

## Install

Download and install the latest release `.xpi` from the Releases page.

## Usage

1. Navigate to a page that loads magazine images (URLs ending in `/extrahigh/0001.bin`, etc.)
2. Wait for the extension icon to show a **PDF** badge
3. Click the extension icon
4. Click **Download PDF**
